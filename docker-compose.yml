services:
  chatgpt-space-merge:
    build: .
    restart: unless-stopped
    ports:
      - "127.0.0.1:18120:18120"
    volumes:
      - chatgpt_space_merge_data:/data
    environment:
      APP_ADDR: 0.0.0.0:18120
      APP_DATA_DIR: /data

volumes:
  chatgpt_space_merge_data:
